{
  "name": "house-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Static house-sitting guide, password-gated, hosted on GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node build/encrypt.mjs",
    "serve": "node --run build && npx --yes serve docs"
  }
}
